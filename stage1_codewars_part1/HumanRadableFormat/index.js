function formatDuration (seconds) {
    let minute = 60,
        hour = 60 * 60,
        day = hour * 24,
        year = day*365;
        
    if (seconds/minute < minute) {
        return `seconds/miny`

    }
    
  }
  